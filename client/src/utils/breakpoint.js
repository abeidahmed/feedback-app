const sizes = {
  xl: 1170,
  lg: 992,
  md: 768,
  sm: 576,
};

export const device = {
  sm: `(min-width: ${sizes.sm})`,
  md: `(min-width: ${sizes.md})`,
  lg: `(min-width: ${sizes.lg})`,
  xl: `(min-width: ${sizes.xl})`,
};
